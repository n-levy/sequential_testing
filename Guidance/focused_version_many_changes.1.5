I would like to revise the focused version (https://sequential-testing.vercel.app/focused).
Please make a plan to implement the following changes, and then commit and push to GitHub:

	1	Change "Binary outcome metric (Bernoulli), modeled as conversion in each arm.” to "Binary outcome metric (Bernoulli), modeled as conversion in each arm. For example: making an order in the first visit during the test.” ﻿
	2	Change the x asix in the plots from “Number of users (n)” to “Time (days)”. In the simulation assumptions and notes, add an assumption that a constant number of users is added to the experiment every day. Then explain that realistically, in many cases we would expect the number of unique users to decrease over time because of repeat visits, but this would not change the learnings that the simulation aims to provide, so we make this simplified assumption. ﻿
	3	In the introduction, add that peeking often happens because of curiosity. If no decisions are made based on those peeks, that is fine. But if the motivation  for peeking is deciding whether to stop the test early, either because there is a clear benefit or a clear harm, then the issues discussed below occur. ﻿
	4	In the math of act 1, do not show the formula for calculating the chances in case of indpendence and then say that actually there is no independence and therefore the formula can not be applied. Simply remove that formula and its discussion. The section is fine without it. Just make sure that the content of the section flows naturally after removing the formula.
	5	In the math of act 1, explain where the value of 1.96 comes from.
	6	 Add a FAQ at the end, with expandable answers by clicking on “+". In the FAQ, explain why the type I error grows slightly, over time, after reaching a certain n. Explain it in two ways. First intuitively. More n adds observations and therefore information, so the CI should become narrower as with the classic formula. However, if we peek continuously, then more observations add more peeking and we need to control for that. Then explain it through the math, following the guidance in the Guidance/eppo_ci_explanation_prompt.md file. ﻿However, state in the beginning that the result is robust to misspecifying n*, so the difference is negligible in most cases and mention a reference for that (search online beforehand).
	7	Please change the title of the Eppo section to “The Eppo Solution (2022).” In the side bar, write “Eppo solution (2022)”. Then add an introduction to the section, explaining that this is the solution described in the 2022 paper by Schmit & Miller (see literature/Schmitt_Miller_2022_EPPO_Sequential_confidence_intervals_with_variance_reduction.pdf for context). We do not know whether this is the exact current implementation in Eppo, but this gives a good example of the way modern analytical platforms implement sequential testing. 
	8	Make “The hybrid approach” a separate act, in between acts 2 and 3. In the introduction, say that this explains a popular ‘hybrid’ approach towards Sequential testing. Then explain the approach and its pros and cons as described in this website: https://www.geteppo.com/blog/comparing-frequentist-vs-bayesian-approaches. Mention that this website is the source. In the simulation assume that our KPI is both a guardrail and the primary outcome metric that we test in the end. Show how the confidence bars are wide during the test and become more narrow the moment the test ends. Use the same design and specifications as the other simulations in the webpage, that the user is already used to seeing. When describing the pros of the hybrid approach, add that in many cases we want to run the test for a round number of weeks to account for weekday effects.
	9	In the “Run 1000 repetitions” simulations, remove the calculation and the display of mean effects. Calculate and display only the share crossing threshold. ﻿However, add a final act (it will be act 5 after adding the hybrid approach act) called “Caution: magnitude error”. In that section, explain that sequential testing increases the ‘magnitude error’ since we condition on high values when stopping (do some online research to understand this better). This creates a larger ‘winners curse’ than the one that the classic confidence intervals create. Therefore, if we stop early we should not pay much attention to the point estimate. This is another reason for preferring the hybrid approach in many cases.  ﻿Then show just the 1000 simulations with the means (the 1000 simulation and table that currently appear at the end of act 2).
	10	When describing the hybrid approach, search online to find out how Eppo approaches this when the Primary outcome metric is also a guardrail. Then describe this in the hybrid approach act in a sub-section, and provide the reference. ﻿
	11	Note: throughout the webpage, use the term confidence intervals without any acronym such as CI.